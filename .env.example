# Feed generator service DID used by app.bsky.feed.generator records.
VITE_BSKY_FEEDGEN_DID=did:web:feeds.moke.me

# Feed generator base URL and secret so Sunnyday can push feed contents on Publish.
# Use the same secret as FEEDGEN_SECRET in feedgen/.env. If unset, Publish only updates Bluesky (feed won't load until you run the feedgen and re-publish).
VITE_FEEDGEN_URL=https://feeds.moke.me
VITE_FEEDGEN_SECRET=
