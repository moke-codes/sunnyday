# DID of this feed generator (must match VITE_BSKY_FEEDGEN_DID in Sunnyday)
FEEDGEN_DID=did:web:feeds.moke.me

# Secret for Sunnyday to push feed contents (set same value as VITE_FEEDGEN_SECRET in Sunnyday .env)
FEEDGEN_SECRET=

# Port (default 5000; nginx proxies to this)
PORT=5000

# Optional: directory for feed-posts.json (default: feedgen/data)
# FEEDGEN_DATA_DIR=/var/lib/sunnyday-feedgen
